{
  "name": "vuelve",
  "version": "0.1.0",
  "description": "Plataforma de fidelización digital multi-rubro para comercios independientes en Chile",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:web\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:web": "cd web && npm run dev",
    "build": "npm run build:backend && npm run build:web",
    "build:backend": "echo 'Backend es Node.js, no requiere build'",
    "build:web": "cd web && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../web && npm install && cd ..",
    "test": "echo 'Tests coming soon'",
    "lint": "echo 'Linting coming soon'"
  },
  "keywords": [
    "fidelización",
    "puntos",
    "comercio",
    "startup",
    "chile"
  ],
  "author": "Vuelve Team",
  "license": "PRIVATE",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
